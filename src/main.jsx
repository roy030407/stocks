import React from 'react'
import ReactDOM from 'react-dom/client'
import StockDashboard from '../frontend/StockDashboard'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StockDashboard />
  </React.StrictMode>,
) 